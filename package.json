{
  "name": "wtc-controller-video",
  "version": "0.1.0",
  "description": "A class to handle the different browser support for videos, specially fullscreen ambient videos.",
  "main": "dist/wtc-controller-video.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "babel src/ --presets babel-preset-es2015 --out-dir dist",
    "watch": "npm run build -- -w -s",
    "demo": "watchify src/wtc-controller-video.js -o dist/demo/bundle.js -v -t [ babelify --presets [ es2015 ] ]",
    "prepublish": "npm run build"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/wethegit/wtc-controller-video.git"
  },
  "keywords": [
    "wtc"
  ],
  "author": "Marlon Ugocioni Marcello <marlon@wethecollective.com> (http://wethecollective.com)",
  "license": "MIT",
  "bugs": {
    "url": "https://github.com/wethegit/wtc-controller-video/issues"
  },
  "homepage": "https://github.com/wethegit/wtc-controller-video#readme",
  "devDependencies": {
    "babel-cli": "^6.18.0",
    "babel-preset-es2015": "^6.18.0",
    "babelify": "^7.3.0"
  },
  "dependencies": {
    "wtc-controller-element": "^1.0.7",
    "wtc-utility-helpers": "^1.1.2"
  }
}
